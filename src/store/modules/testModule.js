/* import axios from 'axios'

export default {
  actions: {
    async fetchPosts () {
      const res = await axios.post('apiURL')
      const posts = await res.json()
    }
  },
  mutations: {},
  state: {
    posts: []
  },
  getters: {
    allPosts (state) {
      return state.posts
    }
  }
}
 */